# Mythic Spellbook — session handoff (2026-08-01)

Paste this into a new session to pick up exactly where this one stopped.

**Repo:** `H:\aiTcgbattler\game-deploy` (single ~12 MB `public/index.html`).
⚠ Do **not** confuse it with `H:\AI Humanity Contained CCG\Files` — a *different* game, unrelated git history, never cross-merge.

---

## 1. LIVE RIGHT NOW: v119t0
`https://playmythicspellbook.play-a3d.workers.dev` — all of today's features, **Foundation Tax = 0 (removed)**, main menu working.
Git `main` is pushed and current (`753ce60fdd` + a live-only tax deploy on top).

## 2. Deploy protocol (do not skip)
1. Bump **three** knobs together: `public/version.txt`, `BUILD_VERSION` in index.html, `CACHE_VERSION` in `public/sw.js`.
2. `npm.cmd run deploy` (Windows: `npm.cmd`, never `npm.ps1`).
3. **Verify the WORKER, never the deploy log** — the log lies ("No updated asset files" can still be a successful deploy). Poll the served page *content* for a new identifier; the edge can serve a stale page for a minute.
4. Before `git add public/index.html`, confirm `head -c 12 public/index.html` is `<!DOCTYPE` (BOM) and **not** `<!--MIN-->` — the deploy minifies in place and restores after.
5. ⚠ `version.txt` has drifted from `BUILD_VERSION` in git history. If you ever roll back, set `version.txt` to the build's real `BUILD_VERSION` or the auto-update check breaks for everyone.

## 3. 🔴 Two open crashes (NOT fixed)
Symptom: heap flat and LOW (5–13%) while the tab dies, and **"Peak decoded images: 0"** on both `title` and `forge`. That's native/GPU memory (decoded bitmaps), invisible to the JS heap watchdog — which fires at 78%+ and therefore can never help.
**Strongest lead:** `_lazyArtOn` and `_decodeBudgetOn` both initialise to `false`. If that layer never switches on, the whole decode-budget system is inert — which explains the `0` on both screens and why the July Forge fix didn't hold. **Investigate read-only first** (`__mg.artStats()`, `__mg.crumbs()`).
❌ Do **not** "fix" it by adding `loading="lazy"` to the hub — I tried that (v119s7), it broke the main menu (missing logo/layout) and was reverted. On-screen art must not be lazy-loaded.
Also open: a **white battle board**, which matches the documented weather-overlay white-out (a stale overlay iframe). Clearing the SW + caches and hard-reloading is the first thing to try.

## 4. 🚚 Convoy trucks — core DONE (inert), gates + UI REMAIN
In `public/index.html`, **uncommitted, not deployed, no call sites** (cannot affect the game).
- Tiers: Scrap Hauler 3/day free · Ironback Runner 6/20 Aza · Ash Convoy Rig 10/60 · Warden Longhaul 15/**99**.
- Allowance is spent **per UNIT** (a 3-unit squad costs 3).
- Functions after `_campFac` (~L61481): `_convoyState/_convoyCapacity/_convoyRemaining/_convoyCanSend/_convoySpend/_convoyGrant/_convoyArt`.
- Cloud whitelist **done both sides** (`__convoy__`): `owned` = UNION (a stale cloud can't delete an Aza-bought truck); `usedToday` = MAX on the same dayKey (device-hopping can't dodge the cap).
**Remaining:** (1) gate `scout`/`raid`/`deepRun` + Resistance Ring Covert Actions — call `_convoyCanSend(n)` BEFORE committing, `_convoySpend(n)` ONLY after, fail closed; (2) Camp panel with the truck picture + sends left; (3) three Vendor Market Aza tiles calling `_convoyGrant()`; (4) admin art editor for `Forge.convoyTruckArt`.
✅ Aza purchases reuse the **existing** Vendor Market rail — `spendSovereigns()` + the `canAffordSov` gate, same as packs. Do not build a new payment path.

## 5. ▶ Card art / card frames not saving
- **RULED OUT:** the `card-art` bucket **is public** and real files serve (HTTP 200). The in-app warning "is the card-art bucket set to Public?" is a generic string and is misleading — don't chase bucket/RLS config.
- There is **no `Forge.*frame*` store at all**, and **no frame key** in the persistence list (~L62736: sprites, cardArt, locationArt, …, hubTileArt). Anything absent from that array cannot survive a reload — the classic "save path that never existed", which is why links *and* uploads both fail.
- ⭐ Click the editor's own **"Why won't it save?"** button first — it's the built-in diagnostic.
- Fix pattern to copy: `chestArt` / `hubTileArt` → a `Forge.<x>` store + the keys array + `idbSet('forge_<x>')` + a catalog publish sentinel.
- ⚠ Most `frame` hits in the code are *sprite animation frames* or *profile cosmetic frames*, not card frames.

## 6. ⚠ Process risks that bit us today
- **Another session was editing this same working tree.** My `git add public/index.html` swept its uncommitted Foundation Tax change (2%→3%) into a commit and deployed it. **Stop the other session before committing from this repo.**
- Deploy cadence matters: seven deploys in a day repeatedly opened the stale-overlay window.

## 7. Shipped today (all live)
3D Ascent roguelite map + visual Campaign Builder + story chaining · grouped effect dropdowns (104 effects, 9 sections) · eight effect primitives (Polycreate material sources, triggered hand abilities, Set Face-Down, Alternate Cost, Sacrifice Self, Sacrifice→Summon, Summon-From-Zone card filter, weather/day-night) · onboarding rewrite · Polycreation modal · Main Hub tutorial fix · Base Construction resource fix · Cinder Shop = Camp only.

## 8. Never verified in a real match
None of the above has been played. The Chrome extension never connected, and the live game requires sign-in (no guest path), so I could not play. **That is the single highest-value next step.**
